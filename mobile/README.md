# Order Tracker Mobile

Flutter client for the ZinCat mini order tracker.

## Run

```bash
flutter pub get
flutter run
```

The app uses `http://localhost:3000/api/v1` by default. Update `lib/core/constants/api_constants.dart` when running on an Android emulator or physical device.
